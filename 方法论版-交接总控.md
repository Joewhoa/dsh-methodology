# 交接总控 · 工作方法论（部署版）

> 本文件是「DSH 工作方法论」的单一真相源：共享约定、坑清单、技能机制、进化路线都在这里。
> 本版已去除原机器的个人身份与求职内容，仅供复刻部署使用。

> **新 agent 接手流程**：读本文件 → 定位项目 → 读该项目交接说明 → 开工。
> **铁律**：任何补丁/变更，做完必须回写本文件或对应项目的交接说明，否则视为「未完成」。这是防止「新 agent 不知情、回头问老大」的根因。
> 基准目录：部署后按实际工作区路径填写。

## 1. 项目清单

| 项目 | 说明 |
|---|---|
| subject-recolor | Agent 驱动的图片批量换色工作流（公开 GitHub，工程化样例） |
| dsh-roam | DSH 手机远程控制台 |
| 账单对账助手 | 规则对账 + LLM 报告 + 轻量 RAG（合成数据） |

## 2. 共享约定（谁用谁读）

- **代理**：git 走 `http://127.0.0.1:7892`（Clash）。被墙才开代理、平时直连；Clash 关闭时 git 全局 `http.proxy` 仍指向 7892 会导致 push 失败，用 `git -c http.proxy= push` 绕过。
- **PowerShell 5.1**：含中文的 `.ps1` 必须用 **UTF-8 带 BOM** 保存，否则 PS 5.1 按 GBK 读 → 乱码 → 语法错误。
- **GitHub token 权限边界**：能建仓库/推送/设 topics/归档，**不能删仓库**（403），**不能 PATCH 已归档仓库**（先解档→改→再归档）。
- **Cloudflare 隧道**：必须带 `--protocol http2 --edge-ip-version 4`（QUIC/7844 被墙、IPv6 edge 超时）。
- **DSH 进程关闭/重启**：必须先提交审批让用户确认，**不得擅自重启**；重启前确认看门狗/托盘在运行。

## 3. 坑清单（新 agent 先读）

1. **⚠️ 改工作区目录名 = 高危**：不要手动改 `~/.dsh/sessions/` 下会话目录名（与会话文件头部 `cwd` 绑定，改了报 `corrupt session log`、DSH 起不来）。遇 `corrupt session log` 先查路径/头部不一致，**绝不删会话**。
2. **PowerShell 5.1 中文乱码** → 用 UTF-8 BOM 重新保存。
3. **桥接启动竞态**：DSH 未就绪时桥接会退出，已加可达性重试（5 次 × 3 秒）。
4. **Cloudflare 1033/530/502** → 加 `--edge-ip-version 4`。
5. **托盘自启静默失败**：先查 Run 键路径是否与脚本实际位置一致。
6. **看门狗/托盘可能没运行**：重启 DSH 前必须先确认有东西能把它拉起。
7. **技能中心面板只扫扁平目录**：面板扫 `~/.dsh/skills`、`<项目>/.dsh/skills`，**不扫** preset 私有目录（preset 技能不出现在面板 ≠ 没注册）。修法：把技能复制到 `~/.dsh/skills/`。

## 4. DSH 技能机制速查

**五个现有 skill：**

- **`cordis-plugin-development`**：写/改/调动态 Cordis 插件（Host 服务、事件、Client UI、工具、版本、审批、诊断）。
- **`editing-cordis-compositions`**：写/改/校验 Cordis 组合（agent 预设、插件行、host vs session）。
- **`delegate-goal`**：委托任务时用「目标契约 + 证据回执 + 风险分级」。
- **`grill-me`**（手动调用）：面试/方案高强度追问。
- **`tdd`**（自动调用）：测试优先开发/修 bug，含 `tests.md`、`mocking.md` 子资源。

**新增 skill 三步走**：①建目录写 `SKILL.md`（frontmatter 只需 `name`+`description`）→ ②放 `~/.dsh/skills/<name>/` → ③触发（用户点名，或任务匹配 description）。

## 5. 进化路线（参考 Cursor Projects · 稳健落地）

目标：从「响应式 + 手动回写 + 半吊子协调」→「目标驱动 + 上下文自动累积 + 纯协调」。5 阶段，稳步推进不搞大重构：

1. **回写机制化** ✅（收尾不可跳过地回写最近进展）
2. **Goal mode**（用 goal 工具挂长期目标，跨会话 resume 能续）
3. **协调者纯度**（只定目标/拆解/委派/验收/抓假报告）
4. **上下文结构化**（暂缓，规模够了再拆）
5. **订阅/主动**（暂缓）

**核心纪律 4 条**：

- **委托纪律**：委派时写「Goal 契约 + 硬约束 + 可判定完成标准」，要求证据型 Receipt，父线程逐条核验、不采信叙述。
- **回写纪律**：做完必回写「最近进展」，否则视为未完成。
- **协调者纯度**：能委派的机械操作不自己干。
- **目标驱动**：长期任务用 goal 工具挂起。

**回写模板**：`- [日期] [项目/工作]：<一句话结果 + 产出链接>`

---

*本文件由「老大」（总控 agent）维护。每次有补丁/新增项目/踩新坑，都回写到这里。*
