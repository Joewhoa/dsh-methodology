# 新设备登录部署（提示词，给新设备 AI 看）

> 本文件在**公开仓库 `dsh-methodology`** 里，无需登录即可读取。
> 目标：完成「GitHub 登录 → 克隆私密仓库 `dsh-handover` → 部署交接总控 → 接手」。

你是这台新机器上的 AI 助手。严格按步骤执行，每步做完向用户汇报结果；遇到卡点停下来问用户，不要瞎猜、不要自作主张。

## 第 0 步：诊断为什么读不到私密仓库

先跑这条命令，看报错类型：

```
git ls-remote https://github.com/Joewhoa/dsh-handover.git
```

- 报 `Could not resolve host` / `Failed to connect` / 超时 → **网络被墙**，走第 1 步。
- 报 `Authentication failed` / `could not read Username` / 弹不出登录框 → **登录问题**，走第 2 步。
- 报 `repository not found` → **账号没权限**，也走第 2 步（token 没勾 repo 权限，或登错账号）。

## 第 1 步：解决网络（仅当第 0 步是网络问题）

1. 确认 Clash / 梯子已开，本地代理一般是 `http://127.0.0.1:7892`。
2. 让 git 走代理：
   ```
   git config --global http.proxy http://127.0.0.1:7892
   git config --global https.proxy http://127.0.0.1:7892
   ```
3. 再跑第 0 步那条 `ls-remote`，通了就继续。
   - 以后关了 Clash 想直连，用 `git -c http.proxy= clone ...` 临时绕过代理。

## 第 2 步：解决登录（用 PAT，最稳，不依赖浏览器弹窗）

1. 让「用户」在任意能上 GitHub 的设备（手机也行）生成一个 Personal Access Token：
   - 打开 https://github.com/settings/tokens
   - 点 Generate new token → Generate new token (classic)
   - Note 随便填（如 dsh-device2）；Expiration 选 90 天或 No expiration
   - 勾选「repo」整栏（其余不用）
   - 拉到最下面 Generate token，复制那串 `ghp_` 开头的 token（只显示一次，先存好）
2. 拿到 token 后，配置凭证存储（二选一）：
   - 简单：`git config --global credential.helper store`（token 明文存本机，够用）
   - 更安全：`git config --global credential.helper manager`（会弹浏览器，能弹就用这个）
3. 克隆时**用户名填 `Joewhoa`，密码填 token**（不是 GitHub 登录密码）：
   ```
   git clone https://github.com/Joewhoa/dsh-handover.git
   ```
   首次问 Username/Password：Username = `Joewhoa`，Password = 粘贴那串 token。

## 第 3 步：进入仓库 + 建本机配置

```
cd dsh-handover
```

新建文件「本机配置.md」（已被 .gitignore 忽略，不会上传），按这台机器填：

```text
<工作区> = 这台机器的 DSH 工作区根目录（绝对路径，例如 D:\Software\AI Tools\DeepSeek Harness）
<本机访问密码> = 你给 dsh-roam 网页设的访问密码
~ = 当前 Windows 用户目录（例如 C:\Users\你的名字）
代理端口 = 7892（Clash）
```

## 第 4 步：读部署清单 + 总控，接手

1. 完整读「新设备部署清单.md」。
2. 完整读「交接总控.md」。读的时候，把里面所有 `<工作区>` 替换成本机配置里填的真实路径，`~` 替换成用户目录。
3. 读完后向用户汇报：「已理解项目清单 / 共享约定 / 坑清单 / 当前进度」，并列出这台机器接下来最该做的 1–3 件事，等用户指令。

## 第 5 步：验证同步（首次回写 + push）

1. 在「交接总控.md」的「最近进展」加一行：
   ```
   - [日期] [新设备部署]：设备 2 已 clone 仓库、建好本机配置、读完总控。
   ```
2. 配置身份并提交推送：
   ```
   git config user.name joewhoa
   git config user.email 1841948421@qq.com
   git add -A
   git commit -m "docs: 设备2 部署完成"
   git push origin main
   ```
3. push 成功后，这台机器就接入同步了。以后：**到机器先 `git pull`，离开前 `git commit + push`**。

## 红线（务必遵守）

- `本机配置.md` 绝对不要 `git add`（已被忽略，但别手动加进去）。
- 任何密码 / token / 密钥不进仓库，只放本机 .env 或本机配置.md。
- 公开仓库（dsh-roam / subject-recolor / billing-reconcile / dsh-methodology）的代码改动走各自仓库，别混进 dsh-handover。
